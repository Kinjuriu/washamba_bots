# State of the campaign — 2026-09-13

Written after a full audit: leaderboard, both active submissions' per-episode records,
all teammate branches, the strategy docs, a fresh local panel, and a replay analysis of
our 20 most recent ladder losses. Numbers below are measured, not recalled.

## Where we actually are

- **Rank 2,606 of 8,820, score 1,358.8.** Top 5 needs ≥ 3,029. Rank 1 is 3,223.6.
- Both active slots (56128408 `router_fam_yarn` control, 56101536 `router_selector`)
  hold **behavior-identical agents** — one slot is a deliberate duplicate from the
  Sep 8/9 control experiment. Win rates 43% and 39%; both traces peaked ~1,550 at
  episode ~20 and have declined since.
- **What matters is Sept 30, not today.** Final ranking is one Bradley-Terry
  tournament over the Oct 1–15 cooldown episodes of the latest 2 submissions
  (`docs/kaggriculture_context.md` §matchmaking). Today's rank is dominated by the
  slot reset; the real problem is the win rate.

## Why the win rate collapsed (measured)

1. **Tape decay is fast and real.** Identical `fam_yarn` code: 71% wins / 2,722 rating
   on Aug 31 (n=77) → 43% wins / 1,353 today (n=152). At matched n=152, the Sep 3
   `router_yuan_nf` submission read 2,377.6 where today's fam_yarn control reads 1,353.5.
   The field re-harvests and surpasses any copied schedule in days
   (MARGIN_LEVERS predicted this: "parity for about a day, not a lead").
2. **The near-copy coin-flip story is stale.** In our 20 most recent losses, opponent
   action agreement over steps 144–400 is 0.00–0.03 (max opening agreement 0.62).
   We now lose to *genuinely different* agents, not mirrors.
3. **Losses are decided mid-season.** 0/20 losses diverge before step 144; 16/20
   diverge decisively on days ~9–15 (harvest/sell/replant window); wins pull away in
   the same window. Worst cases: parity at day 9 → −46,650 by day 30 (ep 108412822),
   −36,708 (ep 108288822). Caveat: the ±2,000-bank divergence metric cannot see
   opening *asset* differences (a late animal / missing quadrant is invisible in bank
   until mid-season) — a step-144 farm-state comparison of the 5 biggest pull-aways
   is running to confirm the "opening is solved" premise before we build on it.

## Local panel (2026-09-13, head_to_head.py)

| comparison | wins | mean Δ |
|---|---|---|
| nf_trim vs fam_yarn (12 seeds) | **24/24** | +6,377 |
| nf_trim vs nf (12 seeds) | 15/24 | +393 |
| nf_trim vs yhay / moon_deficit / meta_lead3 (6 seeds each) | 12/12 each | +6.8k / +20.3k / +15.8k |
| fam_yarn vs same three | 10/12, 12/12, 12/12 | +9.7k / +19.6k / +16.8k |

Both slot candidates pass the pre-submit self-play gate (seed 0, DONE/DONE, ~64k banks).

## Decisions

1. **Restock both slots today**: `agents/router_yuan_nf_trim.py` (Sep 7 build,
   MARGIN_LEVERS' recommended lever set) and `agents/router_yuan_nf.py` (best
   sustained ladder record we own: 2,431.7 over 328 eps, 66% wins, 54 of last 100
   vs a 2,409 field). fam_yarn is beaten 24/24 by nf_trim and currently occupies
   both slots — retire it. Honest caveats: the panel only tested our own older
   lineages; the Yuan-line local-vs-ladder disagreement (cov banks more, rates 350
   worse) is still unexplained; nf_trim vs nf is +393/15-24 — effectively one agent
   in two slots, so treat the nf slot as the seat the hybrid (below) will take.
   Hold the remaining 3 daily submissions.
2. ~~**The 17-day project is the hybrid** (tape opening → state-aware mid-season
   policy).~~ **Overturned same day by the step-144 state check.** The "opening is
   solved" premise was an artifact of the ±2,000-bank divergence metric: land is tied
   at step 144 but **animal fill is already behind** (winners 6/6 filled at day 6 vs
   our 4/6 in 4 of 5 top losses; ~16/16 vs ~8/13 by day 12), and the winners' faster
   $/day (+32–81%) is fertilizer-led compounding off the denser herd. And the winners
   are **not state-reactive**: two different submission IDs (56005477, 55976878)
   produced byte-identical 720-step scripts — the top of this field is a fixed-schedule
   arms race. The revised project: (i) diagnose whether our animal shortfall is the
   tape's own schedule or silent BUY_ANIMAL failures under contested cash, and fix
   accordingly (a repair layer if the latter); (ii) harvest the byte-identical winning
   script (we hold it exactly, twice) as `agents/router_surge.py` and panel it — both
   in flight as of this writing.

   **Diagnosis landed (same day): it is silent replay failure, not the tape's
   schedule.** The tape's fixed-step BUY_ANIMAL orders no-op in contested play —
   live cash at the recorded buy step comes in at $8–90 against fixed prices
   (COW $400 / SHEEP $500) because a live opponent's trading shifts our wheat
   proceeds off the recording; the engine drops the order silently
   (money < price), the router never retries, and the recorded PICKUP/PLACE
   then no-op onto an empty shed slot — the pasture stays empty all season.
   Verified: identical tape actions in wins and losses; wins reach 6/6 filled by
   day 4, the failed episodes plateau at 4–5/6. One order failed at $611 > $500
   purely from sitting behind other market orders in the same turn's list
   (engine settles sequentially). Fix in build as
   `agents/router_yuan_nf_trim_ar.py`: (1) BUY_ANIMAL first in the turn's order
   list, (2) look-ahead cash reserve that defers (not drops) discretionary
   BUY_SEED/BUY_PRODUCT in the day before a scheduled animal buy. One caveat:
   loss 108168443 filled 6/6 and still lost — animal underfill is the dominant
   mechanism in 4/5, not all 5.
3. **Test discipline for the hybrid**: the discriminating harness is the 20 recorded
   loss opponents replayed as fixed tapes on their own seeds (the only local
   opponents that aren't copies of us), plus absolute self-play bank
   (MARGIN_LEVERS rule), plus `experiments/gate.py` (Stephane's pre-ladder gate,
   branch `selector/fam-yarn-continuations`). Mirror-only wins do not ship.

## Same-day build results

- **`agents/router_surge.py`** (harvested byte-identical winning ladder script,
  episodes 108241294/108305356, tape from the $117,351 side): builds and passes the
  self-play gate ($50k mirror banks), banks $136,985 vs `starter` — and **loses 0/36
  to our own lineage locally** (0/24 vs nf_trim, −34,703; 0/12 vs fam_yarn, −30,373).
  The agent that beat us on the ladder collapses against a different market path.
  Conclusion: the fixed-schedule meta is matchup rock-paper-scissors between fragile
  open-loop tapes — whoever's cash assumptions break first loses. Keep surge as a
  panel opponent, not a slot candidate.
- **`agents/router_yuan_nf_trim_ar.py`** (animal cash-reserve rim v1): **no-ship
  as-is.** No local harness reproduces the ladder starvation (baseline lands 13/13
  animals vs surge on 11/12 seeds), so the fix is unvalidated where it matters, and
  it costs −2,573 mean vs starter / −221 absolute self-play when the bug doesn't
  fire. Two measured traps documented in the file: moving BUY_ANIMAL ahead of a SELL
  is −30k (the sell funds the buy); deferring BUY_SEED is −30k+ (desyncs scripted
  PLANT, engine drops the planting outright).
- **Starvation harness + `router_yuan_nf_trim_ar2.py` — done, and the harness
  verdict is the day's real finding.** Three pressure opponents were built/tried
  (`router_surge`, `aggressive_maxxer`, a new `experiments/pressure_opponent.py`
  that front-runs our own tape 3 steps early). Individual BUY_ANIMAL orders DO
  fail locally (verified via shed counts, e.g. seed 0 vs surge, step 89, money
  $49) — but **every seed self-heals to 6/6 by day 6 and 13/13 by day 10** via
  the tape's redundant later buys. The season-long empty-pasture losses need a
  *sustained multi-day* cash trough that no local opponent reproduces. The next
  harness idea is the four remaining loss-opponent tapes in the scratchpad
  `rep/` cache; if those also self-heal, the mechanism depends on the exact
  market path and is unvalidatable locally.
  `ar2` (new file) is strictly conditional — byte-identical banks to nf_trim on
  starter (6 seeds), self-play (4 seeds), surge (12 seeds), and passes the
  pre-submit gate with banks identical to nf_trim's. But per review: "free
  locally" means free against opponents that never trigger it; its fix path has
  possibly never executed (question outstanding), its projection uses quoted
  prices that move mid-order, and a recovered buy still needs the scripted
  PICKUP/PLACE to line up. **Status: diagnosed, fix built, unvalidatable
  locally — ship only as a deliberate ladder experiment in a seat we can afford
  to lose, not as the primary slot.**

  **Correction (same day, follow-up instrumentation):** the harness's one
  "confirmed genuine local failure" (seed 0 vs surge, step 89, "money $49") was
  an off-by-one artifact — `env.steps[i]['observation']` is the observation
  *resulting from* `action_i`, not the one it was decided from; at the correct
  pre-decision state ($159 + same-turn fertilizer sale ≈ $447 vs $400 COW) the
  buy likely succeeded. Consequences: (1) **no genuine local BUY_ANIMAL failure
  has been observed**, and ar2's fix path has never executed on any tested
  seed/opponent (byte-identical actions, 0 diffs over 720 steps); (2) the
  *ladder* diagnosis still stands on its state evidence — day-6/12 pasture-fill
  snapshots (4-5/6 vs 6/6; ~8/13 vs ~16/16) are index-convention-independent,
  and the diagnostic agent verified the pairing convention explicitly — but its
  per-step money figures should be re-derived before anyone builds on them.
  Beware this off-by-one in any future replay forensics.

## Teammate state (so nobody duplicates work)

- Stephane: `router_selector.py` refactor (behavior-identical, live) + `gate.py`
  (unused so far — adopt it as the pre-ladder gate).
- future-centaur/bfg: animal-first from-scratch agent, still −74k/−78k vs
  `route_v20` on seeds 0/8; methodology contribution (timing counters) valuable,
  agent not close. ~10 approaches marked closed in its HANDOFF.
- Billy: moon deficit fix already merged Aug 30; branch superseded.
