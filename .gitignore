.venv/
.ipynb_checkpoints/
__pycache__/
*.pyc

# Episode replays are multi-MB per game — keep them out of history
replay*.json
experiments/*.json

# ...except small benchmark/sweep result summaries (a few KB, not replay
# dumps) that a notebook or report cites as reproducibility evidence —
# these need to travel with the branch, not live only on one machine.
!experiments/pricing_cadence_*.json

# Replay JSONs pulled from the host episode dumps (~30MB each)
replays/
*.json.zip
